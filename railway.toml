[build]
builder = "NIXPACKS"
buildCommand = "npx astro build && npm install -g serve"

[deploy]
startCommand = "serve dist -l $PORT"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
